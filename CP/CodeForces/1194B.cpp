#include <bits/stdc++.h>

using namespace std;

int main()
{
  int T, n, m;
  scanf("%d", &T);

  while(T--)
  {
    scanf("%d %d", &n, &m);
    char a[n+2][m+2];
    getchar();
    for(int i=0; i<n; i++)
        gets(a[i]);

    for(int i=0; i<n; i++)
      for(int j=0; j<m; j++)
  }
}
